{
    "name": "ray-tracer",
    "version": "0.0.0",
    "description": "Ray tracer challenge TypeScript implementation",
    "main": "ray-tracer.js",
    "type": "module",
    "license": "MIT",
    "private": true,
    "devDependencies": {
        "@types/jest": "^26.0.20",
        "jest": "^26.6.3",
        "ts-node": "^9.1.1",
        "typescript": "^4.1.3"
    },
    "scripts": {
        "build": "tsc -p tsconfig.json",
        "jest-esm": "node --experimental-vm-modules node_modules/.bin/jest",
        "test": "yarn build && yarn jest-esm"
    }
}
