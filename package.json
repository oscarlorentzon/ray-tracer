{
    "name": "ray-tracer",
    "version": "0.0.0",
    "description": "Ray tracer challenge TypeScript implementation",
    "module": "build/src/ray-tracer.js",
    "type": "module",
    "license": "MIT",
    "private": true,
    "devDependencies": {
        "@types/jest": "^26.0.20",
        "jest": "^26.6.3",
        "typescript": "^4.1.3"
    },
    "scripts": {
        "animate": "animate() { node build/examples/animation/\"$1\".animation.js; }; animate",
        "build": "tsc -p tsconfig.json",
        "build-examples": "tsc -p tsconfig.examples.json && mkdir -p build/artifacts",
        "clean": "rm -rf build",
        "jest": "node --experimental-vm-modules node_modules/.bin/jest",
        "generate": "generate() { node build/examples/highres/\"$1\".highres.js; }; generate",
        "test": "yarn build && yarn jest"
    }
}
